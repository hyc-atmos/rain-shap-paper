# SHAP feature importance for light-to-moderate and heavy precipitation (GK2A)

This repository provides a script to compute global feature importance using SHAP values
from daily satellite-derived feature CSVs and target CSVs.

## Requirements
```bash
pip install -r requirements.txt

